namespace FabrCore.Client.Components;

/// <summary>
/// Specifies the size of the ChatDock panel.
/// </summary>
public enum ChatDockSize
{
    /// <summary>
    /// Small (default) — 380px wide, 80vh tall for bottom positions.
    /// </summary>
    Small,

    /// <summary>
    /// Medium — ~700px wide, 85vh tall for bottom positions.
    /// </summary>
    Medium,

    /// <summary>
    /// Large — proportional wide, full screen height.
    /// </summary>
    Large
}
